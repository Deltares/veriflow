{
  "$id": "https://deltares.github.io/veriflow/v0/config.schema.json",
  "$defs": {
    "ArchiveKind": {
      "description": "Archive kind.",
      "enum": [
        "open_archive",
        "external_storage_archive"
      ],
      "title": "ArchiveKind",
      "type": "string"
    },
    "CFCompliantNetCDFConfig": {
      "additionalProperties": true,
      "description": "A cf-compliant NetCDF output config element.",
      "properties": {
        "directory": {
          "format": "path",
          "title": "Directory",
          "type": "string"
        },
        "filename": {
          "title": "Filename",
          "type": "string"
        },
        "export_adapter": {
          "const": "cf_compliant_netcdf",
          "title": "Export Adapter",
          "type": "string"
        },
        "force_overwrite": {
          "default": true,
          "title": "Force Overwrite",
          "type": "boolean"
        },
        "crs": {
          "anyOf": [
            {
              "description": "A coordinate reference system as a string compatible with pyproj.CRS.from_string, i.e. a PROJ string, a CRS WKT string, or an authority string such as 'EPSG:4326'. The string is not parsed at configuration time so that pyproj remains an optional dependency; it is only parsed when an actual coordinate transformation is required.",
              "minLength": 1,
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional coordinate reference system for the output. When set, the results' coordinates are reprojected to this CRS before writing.",
          "title": "Crs"
        },
        "conventions": {
          "const": "CF-1.7",
          "default": "CF-1.7",
          "title": "Conventions",
          "type": "string"
        },
        "title": {
          "default": "Verification results created by veriflow",
          "description": "Value for the title attribute in the generated NetCDF. A title will be generated if not provided",
          "title": "Title",
          "type": "string"
        },
        "institution": {
          "description": "Value for the institution attribute in the generated NetCDF.",
          "title": "Institution",
          "type": "string"
        },
        "comment": {
          "default": "Verification results created by veriflow",
          "description": "Value for the comment attribute in the generated NetCDF.",
          "title": "Comment",
          "type": "string"
        }
      },
      "required": [
        "directory",
        "filename",
        "export_adapter",
        "institution"
      ],
      "title": "CFCompliantNetCDFConfig",
      "type": "object"
    },
    "CategoricalScoresConfig": {
      "additionalProperties": true,
      "description": "Config to compute categorical scores, based on an event definition.",
      "properties": {
        "reduce_dims": {
          "description": "Dimensions to reduce over. Can include any combination of: station, forecast_reference_time, lead_time, time, x, y. Only dimensions present in the data will be used. The x, y (spatial) and station dimensions cannot be used together.",
          "items": {
            "enum": [
              "station",
              "forecast_reference_time",
              "lead_time",
              "time",
              "x",
              "y"
            ],
            "type": "string"
          },
          "title": "Reduce Dims",
          "type": "array"
        },
        "score_adapter": {
          "const": "categorical_scores",
          "title": "Score Adapter",
          "type": "string"
        },
        "crs": {
          "anyOf": [
            {
              "description": "A coordinate reference system as a string compatible with pyproj.CRS.from_string, i.e. a PROJ string, a CRS WKT string, or an authority string such as 'EPSG:4326'. The string is not parsed at configuration time so that pyproj remains an optional dependency; it is only parsed when an actual coordinate transformation is required.",
              "minLength": 1,
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional coordinate reference system for score computation. When set, obs and sim coordinates are reprojected to this CRS before computing the score and the results are expressed in it. When omitted, observations and simulations must share the same CRS.",
          "title": "Crs"
        },
        "verification_pair_ids": {
          "default": [],
          "description": "Optional field to select verification_pairs from the general configuration, by providing a list of verification pair ids from the general config. Only these pair ids will be used in the computation of this score.",
          "items": {
            "type": "string"
          },
          "title": "Verification Pair Ids",
          "type": "array"
        },
        "events": {
          "description": "A list of threshold event definitions. For each event, a categorical score will be computed. A threshold event is defined by a threshold and an operator. The threshold is a string that corresponds to a threshold id defined in the configuration. The operator defines how the threshold is applied to the data to create the event. For example, if the threshold is '10' and the operator is 'greater_than', the event will be created by applying the operator to the data and the threshold, i.e. data > 10. ",
          "items": {
            "$ref": "#/$defs/ThresholdEvent"
          },
          "title": "Events",
          "type": "array"
        },
        "scores": {
          "description": "For reference, see: https://scores.readthedocs.io/en/stable/api.html#module-scores.categorical.",
          "items": {
            "$ref": "#/$defs/SupportedCategoricalScores"
          },
          "title": "Scores",
          "type": "array"
        },
        "return_contingency_table": {
          "default": true,
          "description": "Whether to return the contingency table in the output.",
          "title": "Return Contingency Table",
          "type": "boolean"
        }
      },
      "required": [
        "score_adapter",
        "events",
        "scores"
      ],
      "title": "CategoricalScoresConfig",
      "type": "object"
    },
    "ContinuousScoresConfig": {
      "additionalProperties": true,
      "description": "Configure multiple continuous scores.",
      "properties": {
        "reduce_dims": {
          "description": "Dimensions to reduce over. Can include any combination of: station, forecast_reference_time, lead_time, time, x, y. Only dimensions present in the data will be used. The x, y (spatial) and station dimensions cannot be used together.",
          "items": {
            "enum": [
              "station",
              "forecast_reference_time",
              "lead_time",
              "time",
              "x",
              "y"
            ],
            "type": "string"
          },
          "title": "Reduce Dims",
          "type": "array"
        },
        "score_adapter": {
          "const": "continuous_scores",
          "title": "Score Adapter",
          "type": "string"
        },
        "crs": {
          "anyOf": [
            {
              "description": "A coordinate reference system as a string compatible with pyproj.CRS.from_string, i.e. a PROJ string, a CRS WKT string, or an authority string such as 'EPSG:4326'. The string is not parsed at configuration time so that pyproj remains an optional dependency; it is only parsed when an actual coordinate transformation is required.",
              "minLength": 1,
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional coordinate reference system for score computation. When set, obs and sim coordinates are reprojected to this CRS before computing the score and the results are expressed in it. When omitted, observations and simulations must share the same CRS.",
          "title": "Crs"
        },
        "verification_pair_ids": {
          "default": [],
          "description": "Optional field to select verification_pairs from the general configuration, by providing a list of verification pair ids from the general config. Only these pair ids will be used in the computation of this score.",
          "items": {
            "type": "string"
          },
          "title": "Verification Pair Ids",
          "type": "array"
        },
        "scores": {
          "items": {
            "$ref": "#/$defs/SupportedContinuousScore"
          },
          "title": "Scores",
          "type": "array"
        }
      },
      "required": [
        "score_adapter",
        "scores"
      ],
      "title": "ContinuousScoresConfig",
      "type": "object"
    },
    "CrpsCDFConfig": {
      "additionalProperties": true,
      "description": "Configuration for CRPS for CDF.\n\nFor reference, see: https://scores.readthedocs.io/en/stable/api.html#scores.probability.crps_cdf",
      "properties": {
        "reduce_dims": {
          "description": "Dimensions to reduce over. Can include any combination of: station, forecast_reference_time, lead_time, time, x, y. Only dimensions present in the data will be used. The x, y (spatial) and station dimensions cannot be used together.",
          "items": {
            "enum": [
              "station",
              "forecast_reference_time",
              "lead_time",
              "time",
              "x",
              "y"
            ],
            "type": "string"
          },
          "title": "Reduce Dims",
          "type": "array"
        },
        "score_adapter": {
          "const": "crps_cdf",
          "title": "Score Adapter",
          "type": "string"
        },
        "crs": {
          "anyOf": [
            {
              "description": "A coordinate reference system as a string compatible with pyproj.CRS.from_string, i.e. a PROJ string, a CRS WKT string, or an authority string such as 'EPSG:4326'. The string is not parsed at configuration time so that pyproj remains an optional dependency; it is only parsed when an actual coordinate transformation is required.",
              "minLength": 1,
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional coordinate reference system for score computation. When set, obs and sim coordinates are reprojected to this CRS before computing the score and the results are expressed in it. When omitted, observations and simulations must share the same CRS.",
          "title": "Crs"
        },
        "verification_pair_ids": {
          "default": [],
          "description": "Optional field to select verification_pairs from the general configuration, by providing a list of verification pair ids from the general config. Only these pair ids will be used in the computation of this score.",
          "items": {
            "type": "string"
          },
          "title": "Verification Pair Ids",
          "type": "array"
        },
        "integration_method": {
          "default": "exact",
          "description": "The method of integration. 'exact' computes the exact integral, 'trapz' uses a trapezoidal rule and is an approximation of the CRPS.",
          "enum": [
            "exact",
            "trapz"
          ],
          "title": "Integration Method",
          "type": "string"
        }
      },
      "required": [
        "score_adapter"
      ],
      "title": "CrpsCDFConfig",
      "type": "object"
    },
    "CrpsForEnsembleConfig": {
      "additionalProperties": true,
      "description": "Configuration for CRPS for ensemble supporting both point and gridded data.\n\nFor reference, see: See: https://scores.readthedocs.io/en/stable/api.html#scores.probability.crps_for_ensemble",
      "properties": {
        "reduce_dims": {
          "description": "Dimensions to reduce over. Can include any combination of: station, forecast_reference_time, lead_time, time, x, y. Only dimensions present in the data will be used. The x, y (spatial) and station dimensions cannot be used together.",
          "items": {
            "enum": [
              "station",
              "forecast_reference_time",
              "lead_time",
              "time",
              "x",
              "y"
            ],
            "type": "string"
          },
          "title": "Reduce Dims",
          "type": "array"
        },
        "score_adapter": {
          "const": "crps_for_ensemble",
          "title": "Score Adapter",
          "type": "string"
        },
        "crs": {
          "anyOf": [
            {
              "description": "A coordinate reference system as a string compatible with pyproj.CRS.from_string, i.e. a PROJ string, a CRS WKT string, or an authority string such as 'EPSG:4326'. The string is not parsed at configuration time so that pyproj remains an optional dependency; it is only parsed when an actual coordinate transformation is required.",
              "minLength": 1,
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional coordinate reference system for score computation. When set, obs and sim coordinates are reprojected to this CRS before computing the score and the results are expressed in it. When omitted, observations and simulations must share the same CRS.",
          "title": "Crs"
        },
        "verification_pair_ids": {
          "default": [],
          "description": "Optional field to select verification_pairs from the general configuration, by providing a list of verification pair ids from the general config. Only these pair ids will be used in the computation of this score.",
          "items": {
            "type": "string"
          },
          "title": "Verification Pair Ids",
          "type": "array"
        },
        "method": {
          "default": "ecdf",
          "description": "Method to compute the cumulative distribution function from an ensemble.",
          "enum": [
            "ecdf",
            "fair"
          ],
          "title": "Method",
          "type": "string"
        }
      },
      "required": [
        "score_adapter"
      ],
      "title": "CrpsForEnsembleConfig",
      "type": "object"
    },
    "CsvConfig": {
      "additionalProperties": true,
      "description": "A CSV input config element.",
      "properties": {
        "import_adapter": {
          "const": "csv",
          "title": "Import Adapter",
          "type": "string"
        },
        "source_id": {
          "description": "The source indicates the origin of the data. For simulated data this may, for example, refer to the model that produced the data. For observation this may, for example, be simply 'observed' or 'my_validated_observations_database'. The source should start with a letter, consists of only letters, digits and underscores. Spaces or punctuation are disallowed.",
          "minLength": 1,
          "pattern": "^[A-Za-z][A-Za-z0-9_]*$",
          "title": "Source Id",
          "type": "string"
        },
        "data_type": {
          "const": "threshold",
          "title": "Data Type",
          "type": "string"
        },
        "spatial_type": {
          "$ref": "#/$defs/SpatialType",
          "default": "point"
        },
        "directory": {
          "format": "path",
          "title": "Directory",
          "type": "string"
        },
        "filename": {
          "title": "Filename",
          "type": "string"
        },
        "stations": {
          "items": {
            "type": "string"
          },
          "minItems": 1,
          "title": "Stations",
          "type": "array"
        },
        "variables": {
          "items": {
            "type": "string"
          },
          "minItems": 1,
          "title": "Variables",
          "type": "array"
        },
        "thresholds": {
          "items": {
            "type": "string"
          },
          "minItems": 1,
          "title": "Thresholds",
          "type": "array"
        }
      },
      "required": [
        "import_adapter",
        "source_id",
        "data_type",
        "directory",
        "filename",
        "stations",
        "variables",
        "thresholds"
      ],
      "title": "CsvConfig",
      "type": "object"
    },
    "DataType": {
      "description": "Input timeseries data kind.",
      "enum": [
        "observed_historical",
        "simulated_historical",
        "simulated_forecast_single",
        "simulated_forecast_ensemble",
        "simulated_forecast_probabilistic",
        "threshold"
      ],
      "title": "DataType",
      "type": "string"
    },
    "EventOperator": {
      "description": "Event operators.",
      "enum": [
        "greater_than",
        "less_than",
        "greater_than_or_equal_to",
        "less_than_or_equal_to"
      ],
      "title": "EventOperator",
      "type": "string"
    },
    "FewsNetCDFConfig": {
      "additionalProperties": true,
      "description": "A FEWS NetCDF config element.",
      "properties": {
        "directory": {
          "title": "Directory",
          "type": "string"
        },
        "filename_glob": {
          "description": "A valid filename glob, like '*.nc' for all netcdf files.",
          "title": "Filename Glob",
          "type": "string"
        },
        "import_adapter": {
          "const": "fewsnetcdf",
          "title": "Import Adapter",
          "type": "string"
        },
        "source_id": {
          "description": "The source indicates the origin of the data. For simulated data this may, for example, refer to the model that produced the data. For observation this may, for example, be simply 'observed' or 'my_validated_observations_database'. The source should start with a letter, consists of only letters, digits and underscores. Spaces or punctuation are disallowed.",
          "minLength": 1,
          "pattern": "^[A-Za-z][A-Za-z0-9_]*$",
          "title": "Source Id",
          "type": "string"
        },
        "data_type": {
          "$ref": "#/$defs/DataType"
        },
        "spatial_type": {
          "$ref": "#/$defs/SpatialType",
          "default": "point"
        },
        "netcdf_kind": {
          "$ref": "#/$defs/FewsNetCDFKind"
        },
        "station_ids": {
          "anyOf": [
            {
              "items": {
                "type": "string"
              },
              "minItems": 1,
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Station Ids"
        },
        "parameter_ids": {
          "anyOf": [
            {
              "items": {
                "type": "string"
              },
              "minItems": 1,
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Parameter Ids"
        }
      },
      "required": [
        "directory",
        "filename_glob",
        "import_adapter",
        "source_id",
        "data_type",
        "netcdf_kind"
      ],
      "title": "FewsNetCDFConfig",
      "type": "object"
    },
    "FewsNetCDFKind": {
      "description": "FEWS NetCDF kind.",
      "enum": [
        "external_historical",
        "simulated_historical",
        "simulated_forecast_per_forecast_reference_time",
        "simulated_forecast_per_lead_time"
      ],
      "title": "FewsNetCDFKind",
      "type": "string"
    },
    "FewsWebserviceAuthConfig": {
      "additionalProperties": false,
      "description": "Get url, username and password safely from environment variables.\n\nThis config class inherits from :class:`pydantic_settings.BaseSettings`,\nthat will try to infer field values from environment variables.\n\nEnvironment variables:\n\n- ``FEWSWEBSERVICE_URL``: URL of the FEWS webservice (required).\n- ``FEWSWEBSERVICE_USERNAME``: Username for the FEWS webservice (set to \"\" if not required).\n- ``FEWSWEBSERVICE_PASSWORD``: Password for the FEWS webservice (set to \"\" if not required).\n\nsee: https://docs.pydantic.dev/latest/concepts/pydantic_settings/#usage\n\nNotes\n-----\nFor local development, an easy and recommended way to set environment variables is to\nuse a ``.env`` file in the project root with the required environment variables, and they\nwill be automatically loaded when you instantiate this config class. For example:\n\n.. code-block:: python\n\n    from dotenv import load_dotenv\n    load_dotenv()  # Load environment variables from .env file\n\n    # You can check that the variables are loaded correctly\n    import os\n    print(os.getenv(\"FEWSWEBSERVICE_URL\"))",
      "properties": {
        "url": {
          "format": "uri",
          "minLength": 1,
          "title": "Url",
          "type": "string"
        },
        "username": {
          "format": "password",
          "title": "Username",
          "type": "string",
          "writeOnly": true
        },
        "password": {
          "format": "password",
          "title": "Password",
          "type": "string",
          "writeOnly": true
        }
      },
      "required": [
        "url",
        "username",
        "password"
      ],
      "title": "FewsWebserviceAuthConfig",
      "type": "object"
    },
    "FewsWebserviceConfig": {
      "additionalProperties": true,
      "description": "A fews webservice input config element.",
      "properties": {
        "import_adapter": {
          "const": "fewswebservice",
          "title": "Import Adapter",
          "type": "string"
        },
        "source_id": {
          "description": "The source indicates the origin of the data. For simulated data this may, for example, refer to the model that produced the data. For observation this may, for example, be simply 'observed' or 'my_validated_observations_database'. The source should start with a letter, consists of only letters, digits and underscores. Spaces or punctuation are disallowed.",
          "minLength": 1,
          "pattern": "^[A-Za-z][A-Za-z0-9_]*$",
          "title": "Source Id",
          "type": "string"
        },
        "data_type": {
          "$ref": "#/$defs/DataType"
        },
        "spatial_type": {
          "$ref": "#/$defs/SpatialType",
          "default": "point"
        },
        "auth_config": {
          "$ref": "#/$defs/FewsWebserviceAuthConfig"
        },
        "location_ids": {
          "items": {
            "type": "string"
          },
          "minItems": 1,
          "title": "Location Ids",
          "type": "array"
        },
        "parameter_ids": {
          "items": {
            "type": "string"
          },
          "minItems": 1,
          "title": "Parameter Ids",
          "type": "array"
        },
        "module_instance_id": {
          "minLength": 1,
          "title": "Module Instance Id",
          "type": "string"
        },
        "ensemble_id": {
          "anyOf": [
            {
              "minLength": 1,
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Ensemble Id"
        },
        "qualifier_ids": {
          "anyOf": [
            {
              "items": {
                "type": "string"
              },
              "minItems": 1,
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Qualifier Ids"
        },
        "export_id_map": {
          "anyOf": [
            {
              "minLength": 1,
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Export Id Map"
        },
        "webservice_version": {
          "description": "Please specify the version as 'YYYY.01' or 'YYYY.02",
          "pattern": "^\\d{4}\\.(0[1-2])$",
          "title": "Webservice Version",
          "type": "string"
        },
        "archive_kind": {
          "$ref": "#/$defs/ArchiveKind",
          "default": "open_archive",
          "description": "Archive kind. Defaults to a Delft-FEWS Open Archive, which is the Delft-FEWS standard."
        },
        "forecast_retrieval_method": {
          "$ref": "#/$defs/ForecastRetrievalMethod",
          "default": "retrieve_all_forecast_data",
          "description": "Since Delft-FEWS 2025.01, the Delft-FEWS Webservice canretrieve forecasts for specific lead times (lead times). This avoid having to retrieve all forecast data outside of the configured lead times (lead times) for the verification pipeline. If not provided, the method will be automatically determined based on the configured webservice version."
        },
        "max_workers_in_thread_pool": {
          "default": 2,
          "description": "This datasource asynchronously retrieves data from the Delft-FEWS webservice. Define here the maximum workers it can use. Use 5-10 for gentle load on the server-side and keep below 30 to avoid instability and minimize the risk of internal server errors.",
          "title": "Max Workers In Thread Pool",
          "type": "integer"
        }
      },
      "required": [
        "import_adapter",
        "source_id",
        "data_type",
        "location_ids",
        "parameter_ids",
        "module_instance_id",
        "webservice_version"
      ],
      "title": "FewsWebserviceConfig",
      "type": "object"
    },
    "ForecastRetrievalMethod": {
      "description": "Retrieval methods for simulations.",
      "enum": [
        "retrieve_all_forecast_data",
        "retrieve_forecast_data_per_lead_time"
      ],
      "title": "ForecastRetrievalMethod",
      "type": "string"
    },
    "GeneralInfoConfig": {
      "description": "General configuration information that is shared across the pipeline.",
      "properties": {
        "verification_period": {
          "$ref": "#/$defs/VerificationPeriod",
          "description": "The start and end of the verification period."
        },
        "verification_pairs": {
          "description": "Specify pairs for computation of verification metrics. This allows you to verify multiple variables and multiple sources. For example, by specifying two pairs: verify simulated discharge for ModelA and ModelB against observed discharge fromsource Observed.",
          "items": {
            "$ref": "#/$defs/VerificationPair"
          },
          "title": "Verification Pairs",
          "type": "array"
        },
        "lead_times": {
          "anyOf": [
            {
              "$ref": "#/$defs/LeadTimes"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "cache": {
          "anyOf": [
            {
              "$ref": "#/$defs/ZarrCacheConfig"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Veriflow has built-in support for caching data in a local or remote zarr archive store. This allows you to reuse data across multiple runs of the pipeline, which can speed up execution. See the docs for more details and configuration options: https://deltares.github.io/veriflow/"
        }
      },
      "required": [
        "verification_period",
        "verification_pairs"
      ],
      "title": "GeneralInfoConfig",
      "type": "object"
    },
    "IdMap": {
      "additionalProperties": {
        "additionalProperties": {
          "type": "string"
        },
        "type": "object"
      },
      "description": "Mapping from internal IDs to external IDs per data source.",
      "title": "IdMap",
      "type": "object"
    },
    "IdMappingConfig": {
      "description": "Config for mapping external ids to their internal definition.",
      "properties": {
        "variable": {
          "anyOf": [
            {
              "$ref": "#/$defs/IdMap"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Mapping of internal to external definitions per source as a dictionary. The key corresponds to the internal definition and the value is another dictionary with keys corresponding to the source and the value to the external definition. "
        },
        "station": {
          "anyOf": [
            {
              "$ref": "#/$defs/IdMap"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Mapping of internal to external definitions per source as a dictionary. The key corresponds to the internal definition and the value is another dictionary with keys corresponding to the source and the value to the external definition. "
        }
      },
      "title": "IdMappingConfig",
      "type": "object"
    },
    "LeadTimes": {
      "description": "A lead times config element.",
      "properties": {
        "unit": {
          "$ref": "#/$defs/TimeUnits"
        },
        "values": {
          "anyOf": [
            {
              "items": {
                "type": "integer"
              },
              "type": "array"
            },
            {
              "$ref": "#/$defs/Range"
            }
          ],
          "title": "Values"
        }
      },
      "required": [
        "unit",
        "values"
      ],
      "title": "LeadTimes",
      "type": "object"
    },
    "NetCDFConfig": {
      "additionalProperties": true,
      "description": "A NetCDF config element.",
      "properties": {
        "directory": {
          "title": "Directory",
          "type": "string"
        },
        "filename_glob": {
          "description": "A valid filename glob, like '*.nc' for all netcdf files.",
          "title": "Filename Glob",
          "type": "string"
        },
        "import_adapter": {
          "const": "netcdf",
          "title": "Import Adapter",
          "type": "string"
        },
        "source_id": {
          "description": "The source indicates the origin of the data. For simulated data this may, for example, refer to the model that produced the data. For observation this may, for example, be simply 'observed' or 'my_validated_observations_database'. The source should start with a letter, consists of only letters, digits and underscores. Spaces or punctuation are disallowed.",
          "minLength": 1,
          "pattern": "^[A-Za-z][A-Za-z0-9_]*$",
          "title": "Source Id",
          "type": "string"
        },
        "data_type": {
          "$ref": "#/$defs/DataType"
        },
        "spatial_type": {
          "$ref": "#/$defs/SpatialType",
          "default": "point"
        },
        "stations": {
          "anyOf": [
            {
              "items": {
                "type": "string"
              },
              "minItems": 1,
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Stations"
        },
        "variables": {
          "anyOf": [
            {
              "items": {
                "type": "string"
              },
              "minItems": 1,
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Variables"
        }
      },
      "required": [
        "directory",
        "filename_glob",
        "import_adapter",
        "source_id",
        "data_type"
      ],
      "title": "NetCDFConfig",
      "type": "object"
    },
    "Range": {
      "description": "A range.",
      "properties": {
        "start": {
          "title": "Start",
          "type": "integer"
        },
        "end": {
          "title": "End",
          "type": "integer"
        },
        "step": {
          "title": "Step",
          "type": "integer"
        }
      },
      "required": [
        "start",
        "end",
        "step"
      ],
      "title": "Range",
      "type": "object"
    },
    "RankHistogramConfig": {
      "additionalProperties": true,
      "description": "A rank histogram config element supporting both point and gridded ensemble data.",
      "properties": {
        "reduce_dims": {
          "description": "Dimensions to reduce over. Can include any combination of: station, forecast_reference_time, lead_time, time, x, y. Only dimensions present in the data will be used. The x, y (spatial) and station dimensions cannot be used together.",
          "items": {
            "enum": [
              "station",
              "forecast_reference_time",
              "lead_time",
              "time",
              "x",
              "y"
            ],
            "type": "string"
          },
          "title": "Reduce Dims",
          "type": "array"
        },
        "score_adapter": {
          "const": "rank_histogram",
          "title": "Score Adapter",
          "type": "string"
        },
        "crs": {
          "anyOf": [
            {
              "description": "A coordinate reference system as a string compatible with pyproj.CRS.from_string, i.e. a PROJ string, a CRS WKT string, or an authority string such as 'EPSG:4326'. The string is not parsed at configuration time so that pyproj remains an optional dependency; it is only parsed when an actual coordinate transformation is required.",
              "minLength": 1,
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional coordinate reference system for score computation. When set, obs and sim coordinates are reprojected to this CRS before computing the score and the results are expressed in it. When omitted, observations and simulations must share the same CRS.",
          "title": "Crs"
        },
        "verification_pair_ids": {
          "default": [],
          "description": "Optional field to select verification_pairs from the general configuration, by providing a list of verification pair ids from the general config. Only these pair ids will be used in the computation of this score.",
          "items": {
            "type": "string"
          },
          "title": "Verification Pair Ids",
          "type": "array"
        }
      },
      "required": [
        "score_adapter"
      ],
      "title": "RankHistogramConfig",
      "type": "object"
    },
    "ReadWriteMode": {
      "description": "The veriflow cache read/write modes.",
      "enum": [
        "r",
        "rw"
      ],
      "title": "ReadWriteMode",
      "type": "string"
    },
    "S3AuthConfig": {
      "additionalProperties": false,
      "description": "Get S3 credentials and connection info safely from environment variables.\n\nThis config class inherits from :class:`pydantic_settings.BaseSettings`,\nthat will try to infer field values from environment variables.\n\nEnvironment variables (all optional):\n\n- ``S3_ENDPOINT_URL``: Custom S3 endpoint (e.g. for MinIO or non-AWS S3).\n- ``S3_REGION_NAME``: AWS region.\n- ``S3_ACCESS_KEY_ID``: Access key id.\n- ``S3_SECRET_ACCESS_KEY``: Secret access key.\n- ``S3_SESSION_TOKEN``: Session token (for temporary credentials).\n- ``S3_ANON``: Set to ``true`` for anonymous access to public buckets.\n\nFields default to ``None`` (or ``False`` for ``anon``) so that callers can rely on\n``s3fs`` / ``botocore`` falling back to standard AWS credential discovery (e.g.\n``~/.aws/credentials``, instance metadata) when a value is not explicitly set.\n\nsee: https://docs.pydantic.dev/latest/concepts/pydantic_settings/#usage",
      "properties": {
        "endpoint_url": {
          "anyOf": [
            {
              "format": "uri",
              "minLength": 1,
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Endpoint Url"
        },
        "region_name": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Region Name"
        },
        "access_key_id": {
          "anyOf": [
            {
              "format": "password",
              "type": "string",
              "writeOnly": true
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Access Key Id"
        },
        "secret_access_key": {
          "anyOf": [
            {
              "format": "password",
              "type": "string",
              "writeOnly": true
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Secret Access Key"
        },
        "session_token": {
          "anyOf": [
            {
              "format": "password",
              "type": "string",
              "writeOnly": true
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Session Token"
        },
        "anon": {
          "default": false,
          "title": "Anon",
          "type": "boolean"
        }
      },
      "title": "S3AuthConfig",
      "type": "object"
    },
    "SALScoreConfig": {
      "additionalProperties": true,
      "description": "Configuration for the SAL (Structure-Amplitude-Location) spatial score.\n\nApplies to single deterministic gridded forecasts. For reference, see:\nhttps://pysteps.readthedocs.io/en/stable/generated/pysteps.verification.salscores.sal.html",
      "properties": {
        "score_adapter": {
          "const": "sal",
          "title": "Score Adapter",
          "type": "string"
        },
        "crs": {
          "anyOf": [
            {
              "description": "A coordinate reference system as a string compatible with pyproj.CRS.from_string, i.e. a PROJ string, a CRS WKT string, or an authority string such as 'EPSG:4326'. The string is not parsed at configuration time so that pyproj remains an optional dependency; it is only parsed when an actual coordinate transformation is required.",
              "minLength": 1,
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Optional coordinate reference system for score computation. When set, obs and sim coordinates are reprojected to this CRS before computing the score and the results are expressed in it. When omitted, observations and simulations must share the same CRS.",
          "title": "Crs"
        },
        "verification_pair_ids": {
          "default": [],
          "description": "Optional field to select verification_pairs from the general configuration, by providing a list of verification pair ids from the general config. Only these pair ids will be used in the computation of this score.",
          "items": {
            "type": "string"
          },
          "title": "Verification Pair Ids",
          "type": "array"
        },
        "thr_factor": {
          "default": 0.067,
          "description": "Factor by which the threshold quantile is multiplied to obtain the threshold used for object identification. If None in pysteps, no threshold is applied; here a sensible default is used.",
          "title": "Thr Factor",
          "type": "number"
        },
        "thr_quantile": {
          "default": 0.95,
          "description": "Quantile (in [0, 1]) used together with thr_factor to define the threshold for identifying precipitation objects.",
          "maximum": 1.0,
          "minimum": 0.0,
          "title": "Thr Quantile",
          "type": "number"
        }
      },
      "required": [
        "score_adapter"
      ],
      "title": "SALScoreConfig",
      "type": "object"
    },
    "SpatialType": {
      "description": "Spatial structure of a dataset.",
      "enum": [
        "point",
        "gridded"
      ],
      "title": "SpatialType",
      "type": "string"
    },
    "SupportedCategoricalScores": {
      "description": "The supported categorical scores.\n\nExplicitly excluded scores\n- gilberts_skill_score              (identical to equitable_threat_score)\n- threat_score                      (identical to critical_success_index)\n- heidke_skill_score                (identical to cohens_kappa)\n- frequency_bias                    (identical to bias score)\n- fraction_correct                  (identical to accuracy)\n- hanssen_and_kuipers_discriminant  (identical to peirce_skill_score)\n- true_skill_statistic              (identical to peirce_skill_score)\n- yules_q                           (identical to odds_ratio_skill_score)\n- positive_predictive_value         (identical to precision)\n- success_ratio                     (identical to precision)\n- probability_of_detection          (identical to hit_rate)\n- true_positive_rate                (identical to hit rate)\n- sensitivity                       (identical to hit rate)\n- recall                            (identical to hit rate)\n- true_negative_rate                (identical to specificity)\n- probability of false detection    (identical to false alarm rate)",
      "enum": [
        "accuracy",
        "base_rate",
        "bias_score",
        "cohens_kappa",
        "critical_success_index",
        "equitable_threat_score",
        "f1_score",
        "false_alarm_rate",
        "false_alarm_ratio",
        "forecast_rate",
        "peirce_skill_score",
        "hit_rate",
        "negative_predictive_value",
        "odds_ratio",
        "odds_ratio_skill_score",
        "precision",
        "specificity",
        "symmetric_extremal_dependence_index"
      ],
      "title": "SupportedCategoricalScores",
      "type": "string"
    },
    "SupportedContinuousScore": {
      "description": "Supported continuous scores.",
      "enum": [
        "additive_bias",
        "mean_error",
        "mae",
        "mse",
        "rmse",
        "nse",
        "kge"
      ],
      "title": "SupportedContinuousScore",
      "type": "string"
    },
    "SupportedSchemaVersion": {
      "description": "Supported schema versions for the config file.",
      "enum": [
        "v0"
      ],
      "title": "SupportedSchemaVersion",
      "type": "string"
    },
    "ThresholdEvent": {
      "description": "An event definition for a threshold.",
      "properties": {
        "threshold": {
          "description": "Threshold id to use in event definition.",
          "title": "Threshold",
          "type": "string"
        },
        "operator": {
          "$ref": "#/$defs/EventOperator",
          "description": "The operator to use for creating the events."
        }
      },
      "required": [
        "threshold",
        "operator"
      ],
      "title": "ThresholdEvent",
      "type": "object"
    },
    "TimeUnits": {
      "description": "Time unit strings, compatible with numpy datetime64 and timedelta64.",
      "enum": [
        "Y",
        "M",
        "W",
        "D",
        "h",
        "m",
        "s",
        "ns"
      ],
      "title": "TimeUnits",
      "type": "string"
    },
    "VerificationPair": {
      "description": "Configuration for a verification pair.\n\nThe id uniquely identifies the verification pair.\nThe observations_source_id and simulations_source_id fields reference the respective data\nsources and should match with the configured source_id in the datasource configuration.\nThe variable field specifies the (internal; i.e. after id mapping) physical variable to be\nverified.",
      "properties": {
        "id": {
          "title": "Id",
          "type": "string"
        },
        "observations_source_id": {
          "description": "The source indicates the origin of the data. For simulated data this may, for example, refer to the model that produced the data. For observation this may, for example, be simply 'observed' or 'my_validated_observations_database'. The source should start with a letter, consists of only letters, digits and underscores. Spaces or punctuation are disallowed.",
          "minLength": 1,
          "pattern": "^[A-Za-z][A-Za-z0-9_]*$",
          "title": "Observations Source Id",
          "type": "string"
        },
        "simulations_source_id": {
          "description": "The source indicates the origin of the data. For simulated data this may, for example, refer to the model that produced the data. For observation this may, for example, be simply 'observed' or 'my_validated_observations_database'. The source should start with a letter, consists of only letters, digits and underscores. Spaces or punctuation are disallowed.",
          "minLength": 1,
          "pattern": "^[A-Za-z][A-Za-z0-9_]*$",
          "title": "Simulations Source Id",
          "type": "string"
        },
        "variable": {
          "description": "The variable name references a physical variable to be verified. Must match the variable definition in the datasource. IdMapping can be set in the general config, to map external variables to an internal definition. This is needed when you want to verify data from different sources, where the variable definition is not equal.",
          "minLength": 1,
          "title": "Variable",
          "type": "string"
        }
      },
      "required": [
        "id",
        "observations_source_id",
        "simulations_source_id",
        "variable"
      ],
      "title": "VerificationPair",
      "type": "object"
    },
    "VerificationPeriod": {
      "description": "Definition of the verification period.",
      "properties": {
        "start": {
          "description": "YYYY-MM-DD[T]HH:MM[:SS[.ffffff]][Z or [±]HH[:]MM], see: https://docs.pydantic.dev/2.0/usage/types/datetime/#validation-of-datetime-types",
          "format": "date-time",
          "title": "Start",
          "type": "string"
        },
        "end": {
          "description": "YYYY-MM-DD[T]HH:MM[:SS[.ffffff]][Z or [±]HH[:]MM], see: https://docs.pydantic.dev/2.0/usage/types/datetime/#validation-of-datetime-types",
          "format": "date-time",
          "title": "End",
          "type": "string"
        },
        "dimension": {
          "default": "forecast_reference_time",
          "description": "The dimension along which the verification period is defined. Using 'forecast_reference_time' allows for a forecast-centric verification run, whereas 'time' allows for an observation-centric verification run.",
          "enum": [
            "forecast_reference_time",
            "time"
          ],
          "title": "Dimension",
          "type": "string"
        }
      },
      "required": [
        "start",
        "end"
      ],
      "title": "VerificationPeriod",
      "type": "object"
    },
    "ZarrCacheConfig": {
      "description": "Configuration for the veriflow cache.\n\nThe cache will be materialized as a Zarr store on the local filesystem or remote object storage\n(e.g. S3). When configured, the cache will be used to store and retrieve datasets from any\ndatasource. For example: when requesting forecast data from a datasource, and part of the data\nis already cached, the cache will be used to retrieve the cached data and only the missing data\nwill be fetched from the datasource.",
      "properties": {
        "path": {
          "description": "Path to a single Zarr store. Local filesystem path (absolute or relative) or a remote URL such as 's3://bucket/key/store.zarr'.",
          "minLength": 1,
          "title": "Path",
          "type": "string"
        },
        "auth_config": {
          "anyOf": [
            {
              "$ref": "#/$defs/S3AuthConfig"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Authentication configuration for remote stores. Only consulted when 'path' points to an 's3://' location. When the path is remote and this is left unset, credentials are loaded automatically from S3_-prefixed environment variables, so configuring 'auth_config: {}' in YAML is not required."
        },
        "storage_options": {
          "anyOf": [
            {
              "additionalProperties": {
                "type": "string"
              },
              "type": "object"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Additional storage_options forwarded to xr.open_zarr. Merged on top of the options derived from 'auth_config'. Use this for advanced fsspec / s3fs settings not exposed by S3AuthConfig.",
          "title": "Storage Options"
        },
        "consolidated": {
          "anyOf": [
            {
              "type": "boolean"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Whether to use consolidated metadata when opening the store. Forwarded to xr.open_zarr. Default ('None') lets xarray auto-detect.",
          "title": "Consolidated"
        },
        "read_write_mode": {
          "$ref": "#/$defs/ReadWriteMode",
          "default": "r",
          "description": "The read/write mode for the cache."
        }
      },
      "required": [
        "path"
      ],
      "title": "ZarrCacheConfig",
      "type": "object"
    },
    "ZarrConfig": {
      "additionalProperties": true,
      "description": "A Zarr config element.\n\nReads a single Zarr store via :func:`xarray.open_zarr`. The ``path`` may point to a\nlocal directory or a remote location (e.g. ``s3://bucket/key/store.zarr``). When the\npath uses an ``s3://`` URL, credentials and connection details are taken from\n``auth_config`` (an :class:`S3AuthConfig`), which is populated from environment\nvariables prefixed with ``S3_``. Additional ``storage_options`` are merged on top of\nthe ones derived from ``auth_config`` and forwarded to ``xr.open_zarr``.",
      "properties": {
        "path": {
          "description": "Path to a single Zarr store. Local filesystem path (absolute or relative) or a remote URL such as 's3://bucket/key/store.zarr'.",
          "minLength": 1,
          "title": "Path",
          "type": "string"
        },
        "auth_config": {
          "anyOf": [
            {
              "$ref": "#/$defs/S3AuthConfig"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Authentication configuration for remote stores. Only consulted when 'path' points to an 's3://' location. When the path is remote and this is left unset, credentials are loaded automatically from S3_-prefixed environment variables, so configuring 'auth_config: {}' in YAML is not required."
        },
        "storage_options": {
          "anyOf": [
            {
              "additionalProperties": {
                "type": "string"
              },
              "type": "object"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Additional storage_options forwarded to xr.open_zarr. Merged on top of the options derived from 'auth_config'. Use this for advanced fsspec / s3fs settings not exposed by S3AuthConfig.",
          "title": "Storage Options"
        },
        "consolidated": {
          "anyOf": [
            {
              "type": "boolean"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Whether to use consolidated metadata when opening the store. Forwarded to xr.open_zarr. Default ('None') lets xarray auto-detect.",
          "title": "Consolidated"
        },
        "import_adapter": {
          "const": "zarr",
          "title": "Import Adapter",
          "type": "string"
        },
        "source_id": {
          "description": "The source indicates the origin of the data. For simulated data this may, for example, refer to the model that produced the data. For observation this may, for example, be simply 'observed' or 'my_validated_observations_database'. The source should start with a letter, consists of only letters, digits and underscores. Spaces or punctuation are disallowed.",
          "minLength": 1,
          "pattern": "^[A-Za-z][A-Za-z0-9_]*$",
          "title": "Source Id",
          "type": "string"
        },
        "data_type": {
          "$ref": "#/$defs/DataType"
        },
        "spatial_type": {
          "$ref": "#/$defs/SpatialType",
          "default": "point"
        },
        "crs": {
          "anyOf": [
            {
              "description": "A coordinate reference system as a string compatible with pyproj.CRS.from_string, i.e. a PROJ string, a CRS WKT string, or an authority string such as 'EPSG:4326'. The string is not parsed at configuration time so that pyproj remains an optional dependency; it is only parsed when an actual coordinate transformation is required.",
              "minLength": 1,
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Coordinate reference system of the data in the store. Optional: when omitted, the CRS is read from the dataset's 'crs' attribute if present. When set, it takes precedence and is written onto the dataset's 'crs' attribute. Required for gridded data that does not already carry a 'crs' attribute.",
          "title": "Crs"
        },
        "variables": {
          "anyOf": [
            {
              "items": {
                "type": "string"
              },
              "minItems": 1,
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Variables"
        },
        "stations": {
          "anyOf": [
            {
              "items": {
                "type": "string"
              },
              "minItems": 1,
              "type": "array"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Stations"
        }
      },
      "required": [
        "path",
        "import_adapter",
        "source_id",
        "data_type"
      ],
      "title": "ZarrConfig",
      "type": "object"
    }
  },
  "properties": {
    "version": {
      "$ref": "#/$defs/SupportedSchemaVersion",
      "default": "v0"
    },
    "general": {
      "$ref": "#/$defs/GeneralInfoConfig"
    },
    "datasources": {
      "items": {
        "discriminator": {
          "mapping": {
            "csv": "#/$defs/CsvConfig",
            "fewsnetcdf": "#/$defs/FewsNetCDFConfig",
            "fewswebservice": "#/$defs/FewsWebserviceConfig",
            "netcdf": "#/$defs/NetCDFConfig",
            "zarr": "#/$defs/ZarrConfig"
          },
          "propertyName": "import_adapter"
        },
        "oneOf": [
          {
            "$ref": "#/$defs/FewsNetCDFConfig"
          },
          {
            "$ref": "#/$defs/FewsWebserviceConfig"
          },
          {
            "$ref": "#/$defs/CsvConfig"
          },
          {
            "$ref": "#/$defs/NetCDFConfig"
          },
          {
            "$ref": "#/$defs/ZarrConfig"
          }
        ]
      },
      "title": "Datasources",
      "type": "array"
    },
    "scores": {
      "items": {
        "discriminator": {
          "mapping": {
            "categorical_scores": "#/$defs/CategoricalScoresConfig",
            "continuous_scores": "#/$defs/ContinuousScoresConfig",
            "crps_cdf": "#/$defs/CrpsCDFConfig",
            "crps_for_ensemble": "#/$defs/CrpsForEnsembleConfig",
            "rank_histogram": "#/$defs/RankHistogramConfig",
            "sal": "#/$defs/SALScoreConfig"
          },
          "propertyName": "score_adapter"
        },
        "oneOf": [
          {
            "$ref": "#/$defs/CrpsForEnsembleConfig"
          },
          {
            "$ref": "#/$defs/RankHistogramConfig"
          },
          {
            "$ref": "#/$defs/CrpsCDFConfig"
          },
          {
            "$ref": "#/$defs/ContinuousScoresConfig"
          },
          {
            "$ref": "#/$defs/CategoricalScoresConfig"
          },
          {
            "$ref": "#/$defs/SALScoreConfig"
          }
        ]
      },
      "title": "Scores",
      "type": "array"
    },
    "datasinks": {
      "anyOf": [
        {
          "items": {
            "discriminator": {
              "mapping": {
                "cf_compliant_netcdf": "#/$defs/CFCompliantNetCDFConfig"
              },
              "propertyName": "export_adapter"
            },
            "oneOf": [
              {
                "$ref": "#/$defs/CFCompliantNetCDFConfig"
              }
            ]
          },
          "type": "array"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Datasinks"
    },
    "id_mapping": {
      "anyOf": [
        {
          "$ref": "#/$defs/IdMappingConfig"
        },
        {
          "type": "null"
        }
      ],
      "default": null
    }
  },
  "required": [
    "general",
    "datasources",
    "scores"
  ],
  "title": "ConfigSchema",
  "type": "object"
}
